int singleNonDuplicate(vector<int>& arr)
{
	// Write your code here
 /*TC O(n) SC O(1) */
	int i=0;
	while(i!=arr.size()-1){
		if(arr[i]!=arr[i+1]){
			return arr[i];
		}
		else{
			i++;
			i++;
		}
	}
	return arr[arr.size()-1];
}
