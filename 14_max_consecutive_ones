#include <bits/stdc++.h> 
int consecutiveOnes(vector<int>& arr){
    //Write your code here.
    int cnt=0,maxi=0;
    for(int i=0;i<arr.size();i++){
        if(arr[i]==1) cnt++;
        else cnt=0;
        if(cnt>maxi) maxi=cnt;

    }
    return maxi;
}
